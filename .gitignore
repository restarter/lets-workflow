.DS_Store
/reference/
.lets/
/lets/
.idea/
/AGENTS.md
.dolt/
.beads/hooks/
*.db
*.old
dolt-server.*
dolt-monitor.*
.worktrees/
.claude/worktrees/
# MCP server config - may carry secrets (e.g. an API token in its env block); never commit
.mcp.json
.claude/scheduled_tasks*
.beads/
/docs-local/
/scripts/deprecated/

# goreleaser output (created by `goreleaser release --snapshot` / tag-driven CI release)
/dist/

# Beads / Dolt files (added by bd init)
.beads-credential-key
# bd leaves a zero-byte gate lock at the repo root; untracked and unignored it
# fails the release script's clean-tree pre-check (hit at v0.8.2).
.beads.gate.lock
issues.jsonl

# bv (beads viewer) local config and caches
.bv/
.lets

# Private filled-in tracker board instance (real account ids + team PII) - never commit to this public repo.
# planfix-mcp is a user-authored adapter (not a shipped adapter); this keeps its private board out of the repo.
.claude/rules/tracker-planfix-mcp.board.md
