---
description: (deprecated) Alias of /lets:handoff - the old name, removed in a future release; self-contained hand-off brief for another agent, optionally delivered with --send, --open or --codex
argument-hint: "(same as /lets:handoff)"
---

# Review Handoff (deprecated alias)

The old name of `/lets:handoff`, kept so existing habits and links keep working until it is removed in a future release.

First say in one line that `/lets:review-handoff` is deprecated and `/lets:handoff` is the name to use, then `Skill(skill: "lets:handoff", args: "<the arguments exactly as given>")`. No step and no footer of its own: the delegated run's footer is the response's footer.
