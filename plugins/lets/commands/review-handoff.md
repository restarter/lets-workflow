---
description: Alias of /lets:handoff (the old name) - self-contained hand-off brief for another agent, optionally delivered with --codex or --send
argument-hint: "(same as /lets:handoff)"
---

# Review Handoff (alias)

The old name of `/lets:handoff`, kept so existing habits and links keep working.

`Skill(skill: "lets:handoff", args: "<the arguments exactly as given>")` - this file adds nothing: no step and no footer of its own; the delegated run's footer is the response's footer.
