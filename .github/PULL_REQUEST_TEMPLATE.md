## Summary

<!-- What does this PR do and why? One or two sentences. -->

## Task

<!-- Beads task ID if this is tracked work, e.g. lets-xxxxx. Omit for ad-hoc fixes. -->
Task:

## Changes

<!-- Bullet list of the meaningful changes. Skip "edited file X" — the diff shows that. -->
-

## Testing

<!-- How did you verify this? For CLI changes: `make test` + `make build`. -->
- [ ] `make test` passes (if CLI touched)
- [ ] `make build` succeeds (if CLI touched)
- [ ] Manually verified the change

## Notes

<!-- Anything reviewers should know: trade-offs, follow-ups, breaking changes. -->
