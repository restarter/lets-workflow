# lets:managed orca.yaml v1 - regenerated by lets init; remove this line to take ownership
scripts:
  setup: "PATH=\"$PATH:$HOME/.local/bin:/opt/homebrew/bin:/usr/local/bin\"; command -v lets >/dev/null 2>&1 && lets worktree adopt --quiet || echo 'lets: adopt skipped - the session start will bootstrap'"
  archive: "PATH=\"$PATH:$HOME/.local/bin:/opt/homebrew/bin:/usr/local/bin\"; command -v lets >/dev/null 2>&1 && lets worktree release --quiet || true"
